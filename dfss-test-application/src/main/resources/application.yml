spring:
  datasource:
    url: jdbc:mysql://localhost:3316/dfss_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: ShuShun@1558
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    # 控制台打印 SQL
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键类型自增
      id-type: auto
      # 逻辑删除配置（如未使用可忽略）
      logic-delete-value: "1"
      logic-not-delete-value: "0"
